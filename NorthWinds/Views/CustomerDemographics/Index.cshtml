@model IEnumerable<NorthWinds.Models.CustomerDemographic>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CustomerDesc)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CustomerDesc)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CustomerTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CustomerTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CustomerTypeID })
        </td>
    </tr>
}

</table>
